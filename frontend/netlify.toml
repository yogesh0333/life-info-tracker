[build]
  publish = "."
  command = "echo 'No build step needed for static files'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

[context.production.environment]
  NODE_ENV = "production"

